import ChapterNavigation from '../../../components/ui/ChapterNavigation';
import { ChapterCard } from "@/components/ui/ChapterCard";
import { GateTrap } from "@/components/ui/GateTrap";
import { ArchitectShortcut } from "@/components/ui/ArchitectShortcut";
import { SimulatorContainer } from "@/components/ui/SimulatorContainer";
import SystemCalls from "@/components/act-2/SystemCalls";

<div className="max-w-4xl mx-auto space-y-8 pb-24">
  <div className="space-y-2">
    <div className="text-slate-500 font-bold text-sm tracking-widest uppercase">Act 2 — The First Pulse</div>
    <h1 className="text-4xl font-black text-slate-900 tracking-tight">Mission 19: System Calls</h1>
  </div>

  <ChapterCard title="System Briefing" badge="Status: Active" badgeColor="bg-cyan-100 text-cyan-800">
    The Pulse Core is seeking stability in the system calls sector. Use the diagnostic terminal to verify the control path and restore the node.
  </ChapterCard>

  <GateTrap>
    <strong>Diagnostic Trap:</strong> System calls cross the boundary from user mode to kernel mode.
  </GateTrap>

  <ArchitectShortcut>
    <strong>Architect Shortcut:</strong> Treat syscalls as controlled transitions to privileged execution.
  </ArchitectShortcut>

  <SimulatorContainer>
    <SystemCalls />
  </SimulatorContainer>

  <div className="space-y-4">
    <h2 className="text-2xl font-semibold text-slate-900">The Proving Ground</h2>
    <p className="text-sm text-slate-600">Answer the following to demonstrate your understanding of system calls.</p>
    <ul className="list-disc space-y-2 pl-5 text-slate-700">
      <li><strong>Question 1 (NAT):</strong> A system call switches execution to what mode?</li>
      <li><strong>Question 2 (NAT):</strong> Why are syscalls used instead of direct hardware access?</li>
    </ul>
  </div>

  <ChapterNavigation currentChapter={19} />
</div>
